import { AbstractControl } from "@angular/forms";
import { VALIDATORS_PATTERNS } from "../interfaces/validators";

export function arraygroupValidator(control: AbstractControl): {[key: string]: any} | null {

  console.log(control)
  return null;
}
