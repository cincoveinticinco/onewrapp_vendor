import { ArrastarnsoltarDirective } from './arrastarnsoltar.directive';

describe('ArrastarnsoltarDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrastarnsoltarDirective();
    expect(directive).toBeTruthy();
  });
});
